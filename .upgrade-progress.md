# Hermes upgrade to v0.14.0 — progress log

Started: 2026-05-27 14:19 (+0800)
Target:  a91a57fa5 (v0.14.0, 2026-05-16)
Rollback tag prefix: pre-upgrade-20260527-1419/...

Each entry: branch | rebased onto | replayed commits | import smoke | tests | notes


## 2026-05-27 14:35 — BLOCKED on Phase 2.1 (PR4)

### Symptom
First attempted PR4 rebase (commit-by-commit) hit fuzzy-context conflict in
`hermes_cli/config.py` at commit 2/21 (`e3513c3a7 feat: add cognition config defaults`).
The patch's "add new dict key" hunk matched `providers.openrouter` closing brace
instead of top-level DEFAULT_CONFIG (where it belongs); upstream added `bedrock`
in the same lexical area.

Second attempt (whole-PR-as-3way-patch) failed harder:
```
error: agent/smart_model_routing.py: does not exist in index
error: tests/agent/test_smart_model_routing.py: does not exist in index
Applied patch to 'cli.py' with conflicts.
Applied patch to 'cron/scheduler.py' with conflicts.
Applied patch to 'gateway/run.py' with conflicts.
Applied patch to 'hermes_cli/config.py' with conflicts.
Applied patch to 'run_agent.py' with conflicts.
Applied patch to 'tests/agent/test_credential_pool_routing.py' with conflicts.
Applied patch to 'tests/cli/test_cli_provider_resolution.py' with conflicts.
Applied patch to 'tests/run_agent/test_run_agent.py' with conflicts.
```
Patch totally aborted (working tree clean — no partial state).

### Root cause
Upstream commit `424e9f36b refactor: remove smart_model_routing feature (#12732)`
deleted the entire `agent/smart_model_routing.py` module. PR4 (stacked, contains
PR1-PR4 commits) has 2 commits that depend on it:
- `b5ded7ef3 refactor: gate cheap routing behind cognitive mode`
- `a00387d9d refactor: drop unused cognition.default_mode setting`

This is an **architectural directional conflict**, not a mechanical merge issue:
- Your cognition router was designed to gate `smart_model_routing` behavior
- Upstream removed `smart_model_routing` as a feature entirely
- All 17 feat branches inherit PR4's commits and thus this dependency

### What this means for the rest of the stack
- PR4-PR18 all carry the 2 smart_model_routing commits
- merge-tree's "zero file conflicts" prediction was wrong because merge-tree treats
  "file gone in target" as a file-mode conflict, not a content conflict, and our
  count was looking for content conflict markers only

### Options
A. Drop the 2 smart_model_routing commits from PR4 (and from PR5-18), rebase the
   remaining ~19 commits. Cognition router would no longer gate cheap routing
   (functionality loss but architecturally clean).
B. Rewrite cognition router to not depend on smart_model_routing — port the
   "gate cheap routing behind cognitive mode" logic to whatever upstream replaced
   smart_model_routing with (need to investigate: nothing obvious).
C. Pin upgrade target to "just before 424e9f36b" instead of v0.14.0, keep
   smart_model_routing. Loses ~? upstream commits.
D. Wuming reconsiders whether cognition router should keep this functionality
   at all post-upstream change.

### Action
PAUSED. Returned to clean main = v0.14.0, feat/cognition-cleanup-pr4-v2 exists
but empty (just at v0.14.0). All Phase 0 tags intact.

## 2026-05-27 14:48 — Strategy change: rebuild instead of rebase

After 3 failed rebase attempts (commit-by-commit, whole-PR 3way, skip-the-2-smart-routing-commits), the root cause is clear:

- upstream commit 424e9f36b (2026-04-19) removed smart_model_routing wholesale
- PR4 stack designed cognitive_router to gate that feature → wiring layer dead
- mechanical rebase semantically impossible without dropping multiple commits per PR

New plan: docs/plans/2026-05-27-hermes-upgrade-rebuild-實作計畫.md
Strategy: per-PR squashed rebuild on v0.14.0, 4-category file classification,
defer cheap-routing-as-plugin as follow-up.

Workspace state RIGHT NOW:
- main = v0.14.0 (a91a57fa5), local only, not pushed
- fix/codex-stream-typeerror-gpt55 = 5af9c1280 (codex fallback fix), local only
- 94 safety tags pre-upgrade-20260527-1419/...
- runtime remote added (read-only)
- no -v2 branches created yet (PR4-v2 attempt branched then deleted)
- working tree clean (only .upgrade-progress.md untracked)

Next: wuming reviews the rebuild plan, then either he or Claude Code executes.

## 2026-05-27 15:20 — Phase 2 complete: feat/cognitive-stack-v0.14 SUCCESS

Strategy switched to mega-squash (per wuming 2026-05-27 14:50):
> 一個 mega branch，所有功能一股腦進 v0.14.0

### Result
- Branch: feat/cognitive-stack-v0.14
- Commit: c666ef62d
- Size: 62 files changed, 11360 insertions, 4 deletions
- Source: runtime/runtime/active-cognitive-stack (562568eaa, PR18 + 1 image gen commit)
- Target: v0.14.0 (a91a57fa5)

### File categorization actual
- Cat 1 (verbatim copy, new files): 51 files
- Cat 2 (skip — upstream removed): 2 files (smart_model_routing + test)
- Cat 3 (skip — cheap-routing wiring): 4 files restored from main (cli.py, cron/scheduler.py, gateway/run.py, tests/agent/test_credential_pool_routing.py)
- Cat 4 clean 3-way: 8 files (memory_manager, memory_provider, trajectory, hermes_logging, test_memory_provider, test_cli_provider_resolution, test_fast_command, test_hermes_logging)
- Cat 4 manual resolve: 3 files (hermes_cli/config.py, run_agent.py, tests/run_agent/test_run_agent.py)
- Drop (separate feature): 1 file (tools/image_generation_tool.py — Gemini Imagen)

### Manual conflict resolution highlights
- hermes_cli/config.py: cognition block moved to top-level (compression → cognition → auxiliary order); fuzzy-context had it landing inside providers/. Comment block trimmed to drop smart_model_routing example.
- run_agent.py (4 regions): merged HEAD's StreamingContextScrubber/think_scrubber imports + theirs' cognition imports; preserved both HEAD's api_max_retries init + theirs' cognition config init; kept HEAD's _is_entitlement_failure + _decorate_xai_entitlement_error static methods alongside theirs' trajectory metadata wiring + theirs' PR3/PR16/PR18/PR7 post-generation pipeline; used HEAD's _summarize_user_message_for_log helper for the final _save_trajectory call.
- tests/run_agent/test_run_agent.py: HEAD's TestSupportsReasoningExtraBody class + theirs' "Cognitive routing (PR1)" 868-line test bank both retained, joined at the bottom.

### Verification
- import smoke: run_agent + all 11 cognitive submodules import OK
- targeted tests: 182 passing across 14 cognitive test files (cognitive_router, cognition_config, consistency_guard, uncertainty_policy, retrieval_policy, cognition_trace, cognition_trace_report, cognition_observation_benchmark, process_monitor, autonomy_telemetry, memory_ranker, interaction_stance, memory_manager, config_cognition_defaults)
- 0 unresolved conflicts
- syntax OK on all modified files

### Not done (wuming decision pending)
- push main + feat/cognitive-stack-v0.14 to origin (wuming2007 fork)
- runtime handoff (Phase 5)
- broader test suite (test discovery hits xdist worker crash on tests/agent/lsp; doesn't affect cognitive stack)

## 2026-05-27 16:22 — Phase 4 + Phase 5 complete: SHIPPED 🚀

### Phase 4 (push to wuming2007 fork)
- pushed: `feat/cognitive-stack-v0.14` (tip acc7892bf — mega-squash + codex fix)
- pushed: `fix/codex-stream-typeerror-gpt55` (5af9c1280)
- fork main: LEFT ALONE — wuming2007/hermes-agent's main is protected against
  force-push and still carries the old cognitive-stack-on-main lineage as
  archive. Active branch going forward = `feat/cognitive-stack-v0.14`.

### Phase 5 (runtime handoff)
- runtime working tree (codex patch in `M run_agent.py`) stashed: 
  "pre-handoff-20260527 codex patch (already merged in cognitive-stack-v0.14)"
- runtime fetched `wuming/feat/cognitive-stack-v0.14`
- runtime switched to local branch `cognitive-stack-v0.14` tracking the fork branch
- 4 gateways restarted via launchd:
  - main: pid 11342
  - tianji: pid 11362
  - tianquan: pid 11380
  - yuheng: pid 11361
- platform reconnect verified: telegram + slack + feishu all `connected`
- end-to-end `hermes chat -q "say hi in 3 words"` returned "嗨 無名 🌱" in 33s
  (cognitive router, codex fallback, gpt-5.5 all working)

### Final state
- Production: running v0.14.0 + cognitive stack (PR1-18 squashed) + codex fix
- 17 original PR feat branches preserved on both fork and runtime
- 94 pre-upgrade safety tags intact for rollback
- 2 deferred patches saved in docs/plans/smart-routing-plugin-followup/ for plugin work

### Follow-up
- smart-routing-plugin: implement cheap routing as hermes plugin
  using saved patches as reference; cognitive router will surface
  route metadata for the plugin to consume.
- Gemini Imagen support in tools/image_generation_tool.py — separate
  feature, will rebase when needed.

# ─────────────────────────────────────────────
# Hermes upgrade to v0.16.0 — progress log
Started: 2026-06-12 13:37 (+0800)
Target:  3c231eb39 (v0.16.0, 2026-06-05, tag v2026.6.5)
Plan:    docs/plans/2026-06-12-hermes-upgrade-v0.16-實作計畫.md
Rollback tag prefix: pre-upgrade-20260612-1337/...

## codex fixes disposition (v0.16 migration)

- **acc7892bf + 465868339** (accumulator None-iter fallback): DROPPED — superseded by upstream
  cb38ce28c (drop responses.stream() helper) + 486d632cc (None output coercion).
  Verified: accumulator field removed (0 grep hits in run_agent.py + auxiliary_client.py);
  None coercion present at agent/auxiliary_client.py:836 `getattr(final, "output", None) or []`.

- **3e2148349** (stale-detector SSE liveness): NOT PORTED — mitigated via
  providers.openai-codex.stale_timeout_seconds=1800 on runtime config.
  Verified: get_provider_stale_timeout imported and used in run_agent.py:1087;
  config path checks for providers.<id>.stale_timeout_seconds.
  DEFERRED: re-port SSE-liveness onto agent/codex_runtime.py if false
  timeouts reappear (watch agent.log for "stale" kills during deep-mode turns).

- **image_generation_tool test**: File does not exist in v0.16 (already deferred as separate feature).

## image_generation_tool test disposition (v0.16 migration, Task 7 補充)
- tests/tools/test_image_generation_tool.py: NOT PORTED — evaluated against
  v0.16 upstream tools/image_generation_tool.py: 5 failed / 1 passed (test
  bank targets the Gemini Imagen variant we never shipped). Matches the
  v0.14 migration's classification of Gemini Imagen as a separate deferred
  feature; re-add the test together with that feature if/when ported.

## Task 8 — 全面驗證 results (2026-06-12)

Branch: feat/cognitive-stack-v0.16

### Step 1: import smoke
All modules import clean (run_agent, conversation_loop, agent_init, codex_runtime
+ all 14 cognitive modules): `full import smoke OK`

### Step 2: cognitive tests — 19 files, 206 passed, 0 failed (gate ≥182 met)

| File | Result |
|---|---|
| tests/agent/test_cognitive_router.py | 24 passed |
| tests/agent/test_cognition_config.py | 14 passed |
| tests/agent/test_consistency_guard.py | 35 passed |
| tests/agent/test_uncertainty_policy.py | 9 passed |
| tests/agent/test_retrieval_policy.py | 8 passed |
| tests/agent/test_cognition_trace.py | 17 passed |
| tests/agent/test_cognition_trace_report.py | 4 passed |
| tests/agent/test_cognition_observation_benchmark.py | 5 passed |
| tests/agent/test_process_monitor.py | 7 passed |
| tests/agent/test_autonomy_telemetry.py | 8 passed |
| tests/agent/test_memory_ranker.py | 14 passed |
| tests/agent/test_interaction_stance.py | 8 passed |
| tests/agent/test_memory_manager.py | 23 passed |
| tests/agent/test_memory_plasticity.py | 7 passed |
| tests/agent/test_policy_memory.py | 7 passed |
| tests/agent/test_trajectory_metadata.py | 5 passed |
| tests/hermes_cli/test_config_cognition_defaults.py | 6 passed |
| tests/run_agent/test_cognition_observation_benchmark.py | 2 passed (fixture fixed, see below) |
| tests/scripts/test_cognition_trace_report_cli.py | 3 passed |
| **TOTAL** | **206 passed, 0 failed** |

(2 files from the plan's original 21-file list removed as stale — see
"stale cheap-routing tests disposition" below.)

### Step 3: upstream touched files — all green

| File | Result |
|---|---|
| tests/test_hermes_logging.py | 61 passed |
| tests/agent/test_memory_provider.py | 80 passed |
| tests/cli/test_cli_provider_resolution.py | 20 passed (restored to upstream, see disposition) |
| tests/gateway/test_fast_command.py | 4 passed (restored to upstream, see disposition) |
| tests/run_agent/test_run_agent.py (--capture=no -k cognitive bank) | 43 passed |
| tests/run_agent/test_run_agent.py (full run) | 1 failed + mass capture teardown errors — matches the known pre-existing baseline on pristine upstream v2026.6.5; not a regression |

### Step 4: regression spot-check — all green

| File | Result |
|---|---|
| tests/run_agent/test_streaming.py | 36 passed |
| tests/run_agent/test_413_compression.py | 23 passed |
| tests/run_agent/test_context_token_tracking.py | 5 passed |
| tests/hermes_cli/test_config.py | 94 passed |
| tests/agent/test_auxiliary_client.py | 207 passed |
| tests/agent/test_trajectory.py | does not exist in v0.16 (skipped) |

### Step 5: e2e
`timeout 120 uv run hermes chat -q "say hi in 3 words"` → "Hi there, friend",
11s, no cognition-related exceptions.

### Production parity check
`git diff v2026.6.5 HEAD --stat -- cli.py gateway/run.py cron/scheduler.py
agent/smart_model_routing.py` → EMPTY. All three production files are byte-identical
to upstream; agent/smart_model_routing.py does not exist. Consistent with the
v0.14 migration's decision not to port the cheap-routing wiring.

### Fix: tests/run_agent/test_cognition_observation_benchmark.py fixture

Root cause: v0.16 agent_init.py takes the explicit-creds fast path only when
BOTH api_key and base_url are supplied (`if api_key and base_url:` at
agent/agent_init.py:690). The fixture passed api_key only, falling through to
the centralized provider router which raised
`RuntimeError: No LLM provider configured` (agent_init.py:831) on this
unconfigured dev machine.

Change (test-side only): added `base_url="https://openrouter.ai/api/v1"` to the
AIAgent constructor in the `agent()` fixture — the same pattern every other
upstream tests/run_agent/ fixture uses when constructing AIAgent without live
credentials. The client is replaced with MagicMock immediately after, so the
URL value never reaches a network path. No production code changed; no test
assertions weakened. Before: 4 ERRORs at fixture setup; after: 2 passed.

### Stale cheap-routing tests disposition (Task 8)

Upstream removed agent/smart_model_routing.py at 424e9f36b, and the v0.14
migration deliberately did NOT port it (see "Cat 2 (skip — upstream removed)" +
"Cat 3 (skip — cheap-routing wiring)" above; cli.py / cron/scheduler.py /
gateway/run.py were restored to upstream state). The mega-squash nevertheless
copied in test files that exercise that unported wiring; they were broken on
the v0.14 branch too (verified: identical failures when run against c666ef62d's
production code) and were never part of the v0.14 verification list (182 tests
/ 14 files). Disposition:

- tests/cron/test_cognitive_routing.py: REMOVED — imports
  agent.smart_model_routing (ModuleNotFoundError at collection) and
  cron.scheduler._resolve_cron_cognitive_route (never ported).
- tests/gateway/test_fast_command.py: restored to upstream v2026.6.5 — drops
  the 6 stale cheap-routing/cognition-wiring tests our delta appended
  (GatewayRunner._load_cognition_config / _smart_model_routing /
  _resolve_turn_agent_config cognition gating, none of which exist in v0.16).
  The 4 upstream tests remain and pass.
- tests/cli/test_cli_provider_resolution.py: restored to upstream v2026.6.5 —
  our delta was purely the appended cognition-wiring block (6 tests + helpers,
  +174 lines; 5 of 6 fail on pristine v0.16 cli.py for the same reason). The
  20 upstream tests remain and pass.
- tests/cli/test_cli_trajectory_config.py: REMOVED — requires
  HermesCLI.save_trajectories wiring that was never ported (4/4 fail with
  AttributeError on pristine v0.16 cli.py).

All four are deferred to the smart-routing-plugin follow-up
(docs/plans/smart-routing-plugin-followup/), consistent with the v0.14
migration's decision. Re-add the tests together with that feature if/when the
plugin lands.

## 2026-06-12 — Final-review fix: guard second stderr-noise-filter call site

**Root cause (corrected):** Earlier runs attributed "mass capture teardown errors"
(OSError: [Errno 29] Illegal seek) to pre-existing upstream baseline noise. That was
wrong. The actual cause: `hermes_logging.py` has TWO `_install_macos_stderr_noise_filter()`
call sites. The import-time site (~line 242) was correctly guarded with
`if "pytest" not in sys.modules:`, but the second call site inside `setup_logging()`
(~line 345) was UNGUARDED.

v0.16 added an autouse fixture (`tests/hermes_cli/conftest.py:41`) that imports
`hermes_cli.main` during test setup. `hermes_cli/main.py:443` calls `setup_logging()`
at import time → the unguarded call ran and dup2'd a pipe over pytest's seekable
capture tmpfile fd → every subsequent capture snapshot raised OSError: Illegal seek.

**Fix:** Added the identical guard to the `setup_logging()` call site:

```python
# Skipped under pytest: guard matches the import-time call site above.
if "pytest" not in sys.modules:
    _install_macos_stderr_noise_filter()
```

**Verification (all without HERMES_DISABLE_STDERR_NOISE_FILTER):**
- tests/hermes_cli/test_config.py: 94 passed
- tests/hermes_cli/test_config_cognition_defaults.py: 6 passed
- tests/run_agent/test_cognition_observation_benchmark.py: 2 passed
- tests/test_hermes_logging.py: 61 passed (guard-behavior tests still green)
- tests/run_agent/test_run_agent.py: 410 passed, 0 OSError (old "mass capture errors" gone)
- tests/agent/test_memory_manager.py: 23 passed
- Multi-file (test_config_cognition_defaults + test_hermes_logging + test_memory_provider): 147 passed, 0 OSError (old multi-file crash also cured)

## 2026-06-15 18:42 — Task 9 runtime handoff SHIPPED 🚀

### What shipped
- runtime (/Users/wuming/.hermes/hermes-agent) switched: local branch
  `cognitive-stack-v0.16` tracking `wuming/feat/cognitive-stack-v0.16` @ f11cfddef.
  Old branch `cognitive-stack-v0.14` (3e2148349) preserved for rollback.
- Pushed feat/cognitive-stack-v0.16 to fork (origin = wuming2007).

### Config: stale_timeout_seconds applied to ALL 4 live configs (the mitigation
the v0.16 disposition note intended but never deployed)
- /Users/wuming/.hermes/config.yaml (main, gpt-5.5): providers: {} -> openai-codex.stale_timeout_seconds: 1800
- /Users/wuming/.hermes/profiles/{tianji,tianquan}/config.yaml (gpt-5.4): + stale_timeout_seconds: 1800
- /Users/wuming/.hermes/profiles/yuheng/config.yaml (gpt-5.3-codex): + stale_timeout_seconds: 1800
- Backups: <each>.pre-v016-20260615-1832.bak
- NOTE: the stale .hermes-{tianji,tianquan,yuheng}/ dirs (gemini configs) are NOT
  what the gateways read — real live configs are .hermes/profiles/<name>/config.yaml.

### venv sync — IMPORTANT operational learning
- `uv sync --extra all --locked` ALONE strips platform backends: v0.16's [all] is
  deliberately minimal (cron/cli/pty/mcp/homeassistant/sms/acp/google/web/youtube);
  everything else (messaging/slack/feishu/voice/vision/anthropic/tts) is lazy-installed
  at first use via tools/lazy_deps.py (security.allow_lazy_installs=true in their config).
- First sync removed slack-bolt/slack-sdk/sounddevice/tokenizers/anthropic/Pillow/etc.
- CORRECTED sync: `uv sync --extra all --extra messaging --extra feishu --extra vision --locked`
  reinstalled telegram(22.6)/slack-bolt(1.27.0)/slack-sdk(3.40.1)/lark-oapi(1.5.3)/Pillow(12.2.0).
  Re-pinned openai 2.31.0->2.24.0 (drift -> locked), markdown 3.10.2 added, pydantic 2.13.4.
- For FUTURE updates: always pass the platform extras, or rely on lazy-install at first use.

### Gateways restarted (launchctl kickstart -k gui/501/<label>)
- ai.hermes.gateway (main, gpt-5.5) PID 26647
- ai.hermes.gateway-tianji (gpt-5.4) PID 26664
- ai.hermes.gateway-tianquan (gpt-5.4) PID 26686
- ai.hermes.gateway-yuheng (gpt-5.3-codex) PID 26726

### Verification
- Full runtime import smoke OK (run_agent + cognitive stack + gateway + telegram/slack/feishu adapters).
- cognition.enabled=True preserved; providers.openai-codex.stale_timeout_seconds=1800 live.
- Platforms: telegram connected on ALL 4 gateways; slack connected on main (26647).
- e2e: `hermes chat -q "say hi in 3 words"` (main, gpt-5.5/codex) -> "Hi there, friend" in 12s,
  cognitive router active, no stale kill.

### Pre-existing items (NOT migration regressions — verified in v0.14-era logs)
- Slack "app token already in use (PID …)" on tianquan/yuheng: all 4 gateways share ONE
  Slack app token; Socket Mode allows one connection, so only the main gateway holds Slack.
  Seen under v0.14 on 2026-05-27/06-04/06-08. Profiles differentiate via their own Telegram bots.
- Slack missing_scope groups:read (4745x accumulated): Slack app lacks groups:read scope to
  list private channels — non-fatal (messaging works). Fix = add scope in Slack app + reinstall.
- feishu is NOT configured anywhere (config + .env clean); production runs telegram + slack only.
  Earlier "feishu" assumption was wrong; lark-oapi installed but idle/harmless.
- `.env` MESSAGING_CWD deprecation warning (move to config terminal.cwd) — pre-existing, cosmetic.

### Rollback (if needed)
- runtime: `git -C ~/.hermes/hermes-agent switch cognitive-stack-v0.14` + `uv sync --extra all
  --extra messaging --extra feishu --extra vision --locked` + restore the 4 *.pre-v016-*.bak
  configs + kickstart the 4 gateways. Old branch + backups + 94 pre-upgrade tags all intact.

### Observation window
- First 24-48h: watch /Users/wuming/.hermes/logs/gateway.log + profiles/*/logs/ for
  `stale`/`codex stream killed` during deep-mode (code/architecture) turns, and outbound
  message formatting on telegram/slack. If stale kills appear, raise stale_timeout_seconds.

# ─────────────────────────────────────────────
# Hermes upgrade to v0.20.0 — progress log
Started: 2026-08-10 10:32 +0800
Target:  v2026.8.3 (v0.20.0, 2026-08-03)
Plan:    docs/plans/2026-08-10-hermes-upgrade-v0.20-實作計畫.md
Spec:    docs/plans/2026-08-10-hermes-upgrade-v0.20-設計規格.md
Baseline cognitive tests on c70037042: 206 passed
Rollback tag prefix: pre-upgrade-20260810-1032/...
